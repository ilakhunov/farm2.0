# Диагностика проблемы сохранения товара фермера

## ✅ Что уже исправлено

1. **Enum ProductCategory настроен правильно** - добавлена правильная конфигурация для PostgreSQL native enum
2. **Диагностика показывает** - создание товара напрямую в БД работает корректно

## 🔍 Возможные причины проблемы

### 1. Проблема с авторизацией (401 Unauthorized)

**Симптомы:**
- Ошибка "Not authenticated" или "Invalid token"
- Токен не передается или истек

**Проверка:**
```bash
# Проверьте логи backend при попытке создать товар
# Должны увидеть запрос с заголовком Authorization: Bearer <token>
```

**Решение:**
- Убедитесь, что токен сохраняется при авторизации
- Проверьте, что токен передается в заголовке Authorization
- Проверьте срок действия токена (30 минут по умолчанию)

### 2. Проблема с ролью пользователя (403 Forbidden)

**Симптомы:**
- Ошибка "Only farmers can create products"
- Пользователь зарегистрирован не как фермер

**Проверка:**
```bash
# Проверьте роль пользователя в БД
cd backend
poetry run python scripts/test_product_creation.py
```

**Решение:**
- Убедитесь, что при регистрации выбрана роль "farmer"
- Проверьте поле `role` в таблице `users` в БД

### 3. Проблема с валидацией данных (422 Unprocessable Entity)

**Симптомы:**
- Ошибка валидации Pydantic
- Неправильный формат данных

**Проверка:**
- Посмотрите детали ошибки в ответе API
- Проверьте формат категории (должна быть строка: "vegetables", "fruits", etc.)
- Проверьте формат цены и количества (должны быть числа)

**Решение:**
- Убедитесь, что категория передается как строка со значением enum (не именем)
- Проверьте, что цена > 0 и количество >= 0

### 4. Проблема с базой данных (500 Internal Server Error)

**Симптомы:**
- Ошибка при сохранении в БД
- Проблема с enum (уже исправлено)

**Проверка:**
```bash
# Запустите диагностический скрипт
cd backend
poetry run python scripts/test_product_creation.py
```

**Решение:**
- Перезапустите backend сервер после исправления модели
- Убедитесь, что миграции применены
- Проверьте логи PostgreSQL

## 📋 Пошаговая диагностика

### Шаг 1: Проверьте логи backend

```bash
cd backend
export PATH="$HOME/.local/bin:$PATH"
poetry run uvicorn app.main:app --host 0.0.0.0 --reload
```

При попытке создать товар через мобильное приложение смотрите логи:
- Какой HTTP статус возвращается?
- Какая ошибка в ответе?
- Передается ли заголовок Authorization?

### Шаг 2: Проверьте роль пользователя

```bash
cd backend
poetry run python scripts/test_product_creation.py
```

Этот скрипт покажет:
- Есть ли фермеры в БД
- Работает ли создание товара напрямую
- Правильно ли настроены enum

### Шаг 3: Проверьте запрос из мобильного приложения

В мобильном приложении при создании товара проверьте:
1. Передается ли токен в заголовке Authorization?
2. Правильный ли формат данных (категория как строка)?
3. Какая ошибка возвращается?

### Шаг 4: Тестовый запрос через curl

```bash
# Получите токен через авторизацию
TOKEN="your_token_here"

# Попробуйте создать товар
curl -X POST http://localhost:8000/api/v1/products \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый товар",
    "category": "vegetables",
    "price": 100.0,
    "quantity": 10.0,
    "unit": "kg"
  }'
```

## 🛠️ Исправления применены

1. ✅ Модель Product.category настроена для PostgreSQL native enum
2. ✅ Используется values_callable для правильной сериализации enum значений
3. ✅ Диагностический скрипт создан для проверки

## ⚠️ Важно

После исправления модели **обязательно перезапустите backend сервер**, чтобы изменения вступили в силу!

```bash
# Остановите текущий сервер (Ctrl+C)
# Запустите заново
cd backend
export PATH="$HOME/.local/bin:$PATH"
poetry run uvicorn app.main:app --host 0.0.0.0 --reload
```

## 📞 Если проблема сохраняется

Пришлите:
1. Логи backend при попытке создать товар
2. Ответ API (статус код и тело ответа)
3. Роль пользователя в БД
4. Результат выполнения `scripts/test_product_creation.py`


